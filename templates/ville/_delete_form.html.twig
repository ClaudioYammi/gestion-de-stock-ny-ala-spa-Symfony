<form method="post" action="{{ path('app_ville_delete', {'id': ville.id| nzo_encrypt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
        <button class="btn btn-danger btn-sm"><i class="far fa-trash-alt"></i></button>
</form>
