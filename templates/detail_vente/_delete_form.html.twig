<form method="post" action="{{ path('app_detail_vente_delete', {'id': detail_vente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_vente.id) }}">
    <button class="btn">Delete</button>
</form>
