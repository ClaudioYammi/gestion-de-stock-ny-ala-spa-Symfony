<form method="post" action="{{ path('app_inventaire_delete', {'id': inventaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventaire.id) }}">
    <button class="btn">Delete</button>
</form>
