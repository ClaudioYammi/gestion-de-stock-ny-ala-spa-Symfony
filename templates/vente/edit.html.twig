{% extends 'base.html.twig' %}

{% block title %}Modifier les ventes{% endblock %}

{% block body %}
    <h1>Edit Vente</h1>

    {{ include('vente/_form.html.twig', {'button_label': 'Update'}) }}

    
{% endblock %}

{% block widget %}
<div class="row">
    <div class="col-12">
        <div class="d-flex align-items-center">
            <a href="{{ path('app_vente_index') }}" class="btn btn-dark btn-sm"><i class="fas fa-fw fa-arrow-left mr-2"></i>Retour</a>
        </div>
    </div>
</div>
{% endblock %}

{% block widget_droite %}
            {{ include('vente/_delete_form.html.twig') }}
{% endblock %}